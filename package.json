{
  "type": "module",
  "scripts": {
    "setup": "npm ci && npx puppeteer browsers install firefox -y",
    "serve": "tsc && node --disable-warning=ExperimentalWarning ./server/ouroboros.mts"
  },
  "dependencies": {
    "@types/peerjs": "^0.0.30",
    "puppeteer": "^24.2.1"
  }
}
